# Verification Report

> This file is generated by the `openspec-verify-change` skill after apply completes to verify
> consistency between the implementation and specs / design / tasks. A failed check must return
> to the corresponding artifact for correction before verify is rerun.

**Change**: `<change-name>`
**Verified at**: `YYYY-MM-DD HH:mm`
**Verifier**: `<who / which agent>`

---

## 1. Structural Validation (`openspec validate --all --json`)

- [ ] All items have `"valid": true`

**Result**:

```text
<paste a summary of the openspec validate --all output>
```

If any items failed, list their IDs and issues:

| Item | Type | Issues |
|---|---|---|
| — | — | — |

---

## 2. Task Completion (`tasks.md`)

- [ ] All `- [ ]` entries have become `- [x]`

**Incomplete tasks** (if any):

| Task | Reason incomplete | Blocks archive? |
|---|---|---|
| — | — | — |

---

## 3. Delta Spec Sync State

For each capability directory under `openspec/changes/<name>/specs/`, compare it with
`openspec/specs/<capability>/spec.md`:

| Capability | Sync state | Notes |
|---|---|---|
| — | ✓ synced / ✗ pending sync / N/A | — |

---

## 4. Design / Specs Coherence Spot Check

Spot-check whether decisions in `design.md` are reflected in the Requirements and
Scenarios in `specs/*.md`:

| Sample | Design description | Specs counterpart | Gap |
|---|---|---|---|
| — | — | — | — |

**Drift warning** (non-blocking):

- <list any warnings, or write "none">

---

## 5. Implementation Signal

- [ ] The worktree has no unstaged files
- [ ] All relevant commits have been pushed

**Commit range** (if known): `<from-sha>..<to-sha>`

---

## 6. Front-Door Routing Leak Detector (warning, non-blocking)

Design output should not land in `docs/superpowers/specs/` (the brainstorm artifact's
output redirection routes it to `openspec/changes/<name>/brainstorm.md`).

Detection:

```bash
ls docs/superpowers/specs/*.md 2>/dev/null
```

- [ ] No files exist, or any existing files are legitimate remnants from before schema installation

**Leak list** (if any):

| File | Content captured in change? | Recommended action |
|---|---|---|
| — | — | — |

> This does not block archive. Move leaks produced by a new schema-installed cycle into
> `openspec/changes/<name>/brainstorm.md` or `design.md`, then delete the original file.

---

## 7. Deferred Manual Dogfood vs Automated Test Equivalence

For each manual dogfood / smoke task marked `[~]` deferred in plan.md, list
equivalent automated test coverage. If there is no equivalent automated test, treat it as a **real gap**
rather than a reasonable deferral and record it under retrospective Misses.

| Deferred dogfood (plan §) | Equivalent automated test | Coverage assessment | Real gap? |
|---|---|---|---|
| Example: §11.3 `compose up + curl /actuator/health` | `LinebcIntegrationApplicationTests` (Testcontainers, 24s) | Spring context boot + Flyway completion + primary bean injection | ❌ equivalently covered |
| — | — | — | — |

> **Interpretation rules**:
> - "Equivalent" means the automated test's assertion set is a superset of the manual dogfood's expected assertions
> - "Coverage assessment" lists the layers actually exercised (context / DB schema / wiring / HTTP path / etc.)
> - Overall Decision may still be PASS for a row with "Real gap = ✅", but the retrospective must include a follow-up item

> **When this entire section may be blank**: if plan.md has no rows marked `[~]`, this section does not need to be completed (blank means PASS).
> If plan.md contains any `[~]`, every item must be listed here or Overall Decision should be downgraded to FAIL.

---

## Overall Decision

- [ ] ✅ PASS — proceed to finishing-a-development-branch and archive
- [ ] ⚠️ PASS WITH WARNINGS — proceed to later steps with the following caveat: `<explanation>`
- [ ] ❌ FAIL — return to the failed artifact, correct it, and rerun verify

**Next step**:

<describe the next action>
